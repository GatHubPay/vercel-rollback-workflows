# 🎮 Deploy RaspaGanha V2 - Usando Workflow Reutilizável
# Copie este arquivo para: .github/workflows/deploy-caixabox.yml

name: Deploy RaspaGanha V2

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: GatHubPay/vercel-rollback-workflows/.github/workflows/vercel-deploy.yml@main
    with:
      vercelProject: "rasparei"
      projectUrl: "https://rasparei.com/"
      
    secrets:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_TEAM_ID: ${{ secrets.VERCEL_TEAM_ID }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

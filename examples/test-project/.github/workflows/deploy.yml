# 🧪 Exemplo de Uso - Deploy com Rollback Automático
# 
# Este é um exemplo de como usar o workflow em um projeto real

name: Deploy com Rollback Automático

on:
  push:
    branches: [main, master]
  workflow_dispatch: # Permite executar manualmente

jobs:
  deploy:
    uses: ../../.github/workflows/vercel-rollback.yml
    with:
      # Nome do projeto no Vercel (SUBSTITUA pelo seu)
      vercelProject: "meu-projeto-teste"
      
      # Rotas para testar (deixe vazio para auto-detecção)
      routes: "/,/api/health"
      
      # Configurações opcionais
      healthCheckTimeout: 30
      maxRetries: 3
      enablePostDeployMonitoring: false # Desabilitado para teste básico
      
    secrets:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_TEAM_ID: ${{ secrets.VERCEL_TEAM_ID }}
      VERCEL_SCOPE: ${{ secrets.VERCEL_SCOPE }}

# ========================================
# COMO TESTAR ESTE EXEMPLO
# ========================================
# 
# 1. Configure os secrets necessários no GitHub
# 2. Mude o "vercelProject" para um projeto real seu
# 3. Faça push para main/master
# 4. Acompanhe os logs do GitHub Actions
# 5. Verifique as notificações no Telegram
#
# ========================================

name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: |
          echo "✅ Workflow de teste funcionando!"
          echo "🚀 Sistema está operacional"
